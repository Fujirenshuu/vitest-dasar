import {describe, expect, it} from "vitest" //should comment this line, but vscode detect error lol

describe('sample', () => {
    it('should pass 1', () => {
        expect(1).toBe(1)
    })
    it('should pass 2', () => {
        expect(2).toBe(2)
    })
    it('should pass 3', () => {
        expect(3).toBe(3)
    })


})